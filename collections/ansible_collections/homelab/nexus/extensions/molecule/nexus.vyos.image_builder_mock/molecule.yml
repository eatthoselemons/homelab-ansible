---
# Mock test scenario for quick testing without building real VyOS images
# This scenario uses vyos_test_mode to create dummy ISO files for faster testing
dependency:
  name: galaxy
  options:
    requirements-file: requirements.yml

driver:
  name: docker

platforms:
  - name: vyos-image-builder-mock-test
    image: "${MOLECULE_DOCKER_IMAGE:-geerlingguy/docker-ubuntu2404-ansible:latest}"
    pre_build_image: true
    command: "${MOLECULE_COMMAND:-/lib/systemd/systemd}"
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
      - /var/run/docker.sock:/var/run/docker.sock:rw
    cgroupns_mode: host
    privileged: true
    capabilities:
      - SYS_ADMIN
    groups:
      - nexus

provisioner:
  name: ansible
  inventory:
    host_vars:
      vyos-image-builder-mock-test:
        vyos_build_dir: /tmp/vyos-build
        vyos_images_dir: /tmp/vyos-images
        vyos_version: current
        vyos_architecture: amd64
        vyos_build_by: 'molecule-test'
        vyos_build_type: release
        vyos_docker_image: "vyos/vyos-build:current"
        vyos_build_cleanup: true
        ansible_user_id: root
        vyos_test_mode: true  # Enable mock mode for fast testing
  config_options:
    defaults:
      timeout: 300  # 5 minutes for mock testing

verifier:
  name: ansible
